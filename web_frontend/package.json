{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "tsc -p tsconfig.json || true",
    "dev": "PORT=${NEXT_PUBLIC_PORT:-3000} HOST=0.0.0.0 node -e \"require('./dist/startup-log.js')\" || true && next dev -p ${NEXT_PUBLIC_PORT:-3000} -H 0.0.0.0",
    "build": "next build",
    "prestart": "tsc -p tsconfig.json || true",
    "start": "PORT=${NEXT_PUBLIC_PORT:-3000} HOST=0.0.0.0 node -e \"require('./dist/startup-log.js')\" || true && next start -p ${NEXT_PUBLIC_PORT:-3000} -H 0.0.0.0",
    "export": "next build && next export -o out",
    "lint": "next lint"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "next": "15.2.3"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "15.2.1",
    "@eslint/eslintrc": "^3",
    "ts-node": "^10.9.2"
  }
}
